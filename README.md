# aiops-observability

End-to-end demo: OpenTelemetry microservices + Grafana + AIOps Detector (anomaly scoring + RCA stub).
Runs locally on Mac using **kind + helm**.

## Prerequisites (Mac)
- Docker Desktop installed & running
- Homebrew installed

Install CLI tools:
```bash
./scripts/bootstrap_mac.sh
