apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aiops-detector.fullname" . }}-test-connection"
  labels:
    {{- include "aiops-detector.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aiops-detector.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
