apiVersion: v1
kind: Service
metadata:
  name: {{ include "aiops-detector.fullname" . }}
  labels:
    {{- include "aiops-detector.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aiops-detector.selectorLabels" . | nindent 4 }}
